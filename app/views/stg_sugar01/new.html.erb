
<h3>Ajouter un nouveau enregistrement dans stg_sugar01</h3>

<%= link_to('<< Back to List', {:controller => 'sugar01', :action => 'list'}, :class => 'back-link') %>

<div class="subject new">
  <%= simple_form_for(:stg_sugar01, :html => { :multipart => true }, :url => {:action => 'create'}) do |f| %>
    <table summary="Stg_produits form fields">
      <%= f.input :month_sk, label: 'Année'  %>
      <%= f.input :ctry_sk, label: 'Pays', :as => :select, :collection => DimCountrie.all %>
      <%= f.input :product_sk, label: 'Produit', :as => :select, :collection => DimProduit.all%>
      <%= f.input :trans_sk, label: 'Type de transaction', :as => :select, :collection => DimTransaction.all %>
      <%= f.input :volume_kg, label: 'Volume (kg)' %>
      <%= f.input :price_xof, label: 'Prix (CFA)' %>
      <%= f.button :submit %>

    </table>
    <div class='form-buttons'>
      <%= submit_tag('Enregistrer') %>
    </div>

  <% end %>
</div>