<% @page_title= 'Publications List' %>

<h4>Les Etudes d'impact</h4>

<%= link_to('Add New Subject', {:action => 'new'}, :class => 'action new') %>

 <table class="listing" summary="Subject list">
    <tr class="header">
      <th>N°</th>
      <th>Titre</th>
    </tr>
    <% @publications.each do |publication| %>
        <tr>
          <td><%= publication.numpublication %></td>
          <td class="left"><%= publication.titrepublication %></td>
          <td class="actions">
            <%= link_to('Voir', {:action => 'show', :id => publication.id}, :class => 'action show') %>
            <!--  non visible
            <%= link_to("Edit", {:action => 'edit', :id => publication.id}, :class => 'action edit') %>
            <%= link_to('Delete', {:action => 'delete', :id => publication.id}, :class => 'action delete') %>
            -->
          </td>
        </tr>
    <% end %>
  </table>